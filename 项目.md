# CI/CD 流水线自动搭建平台

## 项目概述

一站式 CI/CD 流水线自动搭建平台，支持 7 种主流流水线工具、6 种项目类型、3 种部署方式，可自动安装 CI/CD 工具并配置流水线，支持穿透堡垒机/零信任网络访问目标服务器。

**代码规模**：约 9,000 行（后端 6,000+ 行 Python，前端 2,400 行 Vue）

**仓库地址**：https://github.com/tianlanshuilan/autocicd

---

## 核心功能

### 1. 流水线配置文件生成

根据用户输入的项目信息，自动生成对应工具的流水线配置文件。

| 流水线工具 | 生成文件 | 说明 |
|-----------|---------|------|
| Jenkins | Jenkinsfile | 含构建、测试、部署、备份全流程 |
| 阿里云效 | .aliyunxiao.yml | 阿里云 Flow 流水线 |
| 华为云 CodeArts | .huaweicloud.yml | 华为云流水线 |
| 腾讯云 TKE | .tencent.yml | 腾讯云流水线 |
| GitHub Actions | .github/workflows/ci.yml + deploy.yml | CI + 部署双 workflow |
| GitLab CI | .gitlab-ci.yml | 含 build/test/deploy 三阶段 |
| Runner | setup-runner.sh + config.toml | GitLab Runner / GitHub Runner 安装注册 |

### 2. 支持的项目类型

| 项目类型 | 构建工具 | 测试命令 | Docker 支持 |
|---------|---------|---------|------------|
| Java Maven | mvn clean package | mvn test | ✅ 多阶段构建 |
| Java Gradle | gradle bootJar | gradle test | ✅ 多阶段构建 |
| Vue.js | npm run build | npm test | ✅ Nginx 部署 |
| React | npm run build | npm test | ✅ Nginx 部署 |
| Python | pip install | pytest | ✅ |
| Go | go build | go test ./... | ✅ 多阶段构建 |

### 3. 部署方式

| 方式 | 说明 |
|------|------|
| Docker 容器部署 | 生成 Dockerfile，构建镜像并部署 |
| 直接部署 | SSH 传输产物到目标服务器直接运行 |
| 应用服务器部署 | 支持东方通 TongWeb 等国产应用服务器 |

### 4. 自动化搭建（一键部署）

通过 WebSocket 实时通信，自动完成以下 7 步：

| 步骤 | 说明 |
|------|------|
| ① 克隆代码仓库 | 使用 Git 凭据拉取源代码 |
| ② 生成配置文件 | 根据项目信息生成流水线配置 |
| ③ 推送配置到仓库 | 将配置文件推送到 Git 仓库 |
| ④ 选择集成分支 | 配置多分支集成策略 |
| ⑤ 连接目标服务器 | SSH 连接（支持堡垒机/跳板机穿透） |
| ⑥ 安装 CI/CD 工具 | 自动安装 Jenkins/Runner 并配置环境 |
| ⑦ 配置流水线 | 创建 Jenkins Job 或注册 Runner |

### 5. 堡垒机/零信任网络穿透

**核心特性**：生成的 Pipeline 本身支持穿透堡垒机访问目标服务器。

- 目标服务器在堡垒机/零信任网络内时，可配置堡垒机跳转
- SSH 使用 `-J`（ProxyJump）选项穿透堡垒机
- 支持密码和 SSH 密钥两种认证方式
- 适用于 Jenkins、GitLab CI、GitHub Actions 所有工具

```
[CI/CD 工具服务器] --SSH -J 堡垒机--> [目标部署服务器（零信任内网）]
```

### 6. 多分支发布流程

支持多分支并行构建 + 审批门禁 + 自动合并 + 回滚：

| 策略 | 说明 |
|------|------|
| auto_merge | 测试通过后自动合并到主分支 |
| manual_merge | 审批通过后手动合并 |
| no_merge | 各分支独立部署，不合并 |

包含回滚脚本（rollback.sh）和发布说明文档。

### 7. 智能部署建议

根据项目类型、工具选择、网络环境等，自动生成部署建议：
- CI/CD 服务器部署位置建议
- 部署方式推荐
- 网络方案建议
- 安全提示和最佳实践

### 8. 离线安装包支持

针对无网络环境（如内网、隔离区），提供离线安装包兜底方案：

| 工具 | 架构 | 文件 |
|------|------|------|
| Jenkins | 通用 | jenkins.war (96MB) |
| JDK 17 | x86_64 | openjdk.tar.gz (178MB) |
| JDK 17 | aarch64 | openjdk.tar.gz (177MB) |
| JDK 8 | x86_64 | openjdk8.tar.gz (98MB) |
| JDK 8 | aarch64 | openjdk8.tar.gz (97MB) |
| Docker | amd64 | docker.tgz (67MB) |
| Docker | arm64 | docker.tgz (61MB) |
| GitLab Runner | amd64 | gitlab-runner (118MB) |
| GitLab Runner | arm64 | gitlab-runner (110MB) |

安装策略：在线安装 → 离线安装包 → 手动安装提示（三级降级）

---

## 运行方式

### 方式一：Web 服务（生产模式）

```bash
python standalone.py
# 访问 http://localhost:8000
```

后端 FastAPI 同时提供 API 和前端静态文件，无需单独启动前端。

### 方式二：开发模式

```bash
# 终端 1 - 后端（端口 8000）
cd backend && uvicorn main:app --reload --port 8000

# 终端 2 - 前端（端口 3000）
cd frontend && npm run dev
```

### 方式三：桌面应用

```bash
python desktop/app.py
# 弹出原生窗口应用
```

基于 pywebview，内嵌 FastAPI 服务，独立窗口运行。

---

## 项目结构

```
auto-cicd/
├── backend/                    # 后端 FastAPI 服务
│   ├── main.py                 # API 入口，数据模型定义
│   ├── recommendations.py      # 智能部署建议引擎
│   ├── generators/             # 流水线配置生成器
│   │   ├── jenkins.py          # Jenkins（Jenkinsfile）
│   │   ├── aliyun.py           # 阿里云效
│   │   ├── huawei.py           # 华为云 CodeArts
│   │   ├── tencent.py          # 腾讯云 TKE
│   │   ├── github.py           # GitHub Actions
│   │   ├── gitlab.py           # GitLab CI
│   │   ├── runner.py           # GitLab Runner / GitHub Runner
│   │   ├── tongweb.py          # 东方通 TongWeb 应用服务器
│   │   └── workflow.py         # 多分支发布流程（审批/合并/回滚）
│   └── pipeline/               # 自动化搭建引擎
│       ├── engine.py           # 流水线编排（7 步自动化）
│       ├── ssh_ops.py          # SSH 操作（连接、安装、部署）
│       ├── git_ops.py          # Git 操作（克隆、推送）
│       └── credential.py       # 凭据管理
├── frontend/                   # 前端 Vue 3 应用
│   ├── src/
│   │   ├── views/MainView.vue  # 主界面（表单 + 结果 + 进度）
│   │   ├── api/index.js        # API 调用封装
│   │   └── App.vue             # 应用入口
│   └── vite.config.js          # Vite 构建配置
├── desktop/                    # 桌面应用
│   ├── app.py                  # pywebview 入口
│   ├── icons/                  # 多平台图标
│   └── installer/              # 安装包构建脚本
├── standalone.py               # 生产模式入口（合并前后端）
├── bundled-tools/              # 离线安装包
│   ├── jenkins/jenkins.war
│   ├── jdk/{x86_64,aarch64}/
│   ├── docker/{amd64,arm64}/
│   └── runner/{amd64,arm64,arm}/
├── .github/workflows/          # GitHub Actions 自动构建
│   ├── build.yml               # 服务器版构建
│   └── build-desktop.yml       # 桌面版构建
├── auto-cicd.spec              # PyInstaller 服务器版打包
├── auto-cicd-desktop.spec      # PyInstaller 桌面版打包
└── requirements.txt            # Python 依赖
```

---

## 技术栈

| 层级 | 技术 |
|------|------|
| 前端 | Vue 3 + Vite |
| 后端 | Python FastAPI + uvicorn |
| SSH | paramiko（支持跳板机/堡垒机） |
| 桌面 | pywebview + PyInstaller |
| CI/CD | GitHub Actions（三平台自动构建） |
| 模板 | Jinja2（配置文件生成） |

---

## 分发方式

### 服务器版（build.yml）

| 平台 | 产物 | 说明 |
|------|------|------|
| Linux | auto-cicd-linux-x64 | ELF 可执行文件 |
| macOS | auto-cicd-macos-x64 | Mach-O 可执行文件 |
| Windows | auto-cicd-windows-x64.exe | PE 可执行文件 |

### 桌面版（build-desktop.yml）

| 平台 | 产物 | 说明 |
|------|------|------|
| macOS | AutoCICD-macOS.dmg | 双击挂载安装 |
| Windows | AutoCICD-Windows.exe | 双击运行 |
| Linux | AutoCICD-Linux / .deb | 直接运行或 dpkg 安装 |

### 触发构建

```bash
git tag v1.0.6 && git push origin v1.0.6
```

---

## 前端界面模块

| 模块 | 说明 |
|------|------|
| 1. 流水线工具 | Jenkins / 阿里云效 / 华为云 / 腾讯云 / GitHub / GitLab / Runner |
| 2. 项目类型 | Java Maven / Gradle / Vue / React / Python / Go |
| 3. 部署方式 | Docker / 直接部署 / 应用服务器 |
| 4. 项目信息 | 仓库 URL、项目名、分支（支持多分支）、端口、JDK/Node 版本 |
| 5. Git 认证 | 密码 / SSH 密钥 |
| 6. 目标服务器 | 地址、端口、用户名、认证方式、部署路径、堡垒机配置 |
| 7. 网络访问链路 | 中继/堡垒机/零信任多跳链路配置 |
| 8. CI/CD 工具部署位置 | 专用服务器 / 目标服务器 / 托管服务 |
| 9. 部署建议 | 智能推荐服务器配置、网络方案、安全建议 |


## 验证分析要求
- 必须要对项目进行分析认证
- 验证是否真正做到根据配置自动搭建流水线的功能
- 而且配置信息提示齐全完整，不存在缺少配置信息而无法搭建的问题，必须的配置信息必须提示填入才能进行搭建
- 而且对于跨云服务商的搭建一定要保证能够实现
- 需要通过零信任或者vpn+堡垒机的方式是否能够保证可以访问服务器
- 需要哪些参数、配置信息，必须要求完整齐全


## 验证分析报告

### 验证项 1：自动搭建流水线功能真实性 ✅ 通过

逐项追踪完整执行链路（`backend/pipeline/engine.py`）：

| 步骤 | 验证点 | 结论 |
|------|--------|------|
| 1. git_clone | 真实调用 GitOps 执行 `git clone`，多分支时克隆全部分支 | ✅ 真实实现 |
| 2. branch_select | 列出真实远程分支（git ls-remote），交互式弹窗选择多分支与发布策略 | ✅ 真实实现 |
| 3. generate_config | 分支/策略先于配置生成，多分支选择真实反映到 Jenkinsfile/YAML | ✅ 真实实现（此前顺序 Bug 已修复） |
| 4. git_push | 配置推送回仓库，凭据注入 URL | ✅ 真实实现 |
| 5. ssh_connect | Paramiko 建立 SSH 连接（支持多跳链路），凭据缺失时弹窗补充 | ✅ 真实实现 |
| 6. install_tool | 检测系统架构/发行版/国产 OS，三级降级安装（在线→国内镜像→离线包） | ✅ 真实实现 |
| 7. configure_pipeline | 上传配置、注册 Runner、创建 Job | ✅ 真实实现 |

### 验证项 2：配置信息提示齐全与必填校验 ✅ 通过（已补强）

**提交前必填校验**（本次新增）：
- 仓库 URL、项目名称、目标服务器地址、默认分支 → 缺失时按钮禁用 + 提交拦截
- 专用服务器模式 → 工具服务器地址必填
- 启用堡垒机 → 堡垒机地址和用户名必填

**执行时动态凭据弹窗**：Git 密码/密钥、服务器密码/密钥、工具服务器凭据、各跳板机凭据缺失时均弹窗要求补充输入，取消则明确失败并提示原因，不存在"静默失败"。

**本次验证发现并修复的缺陷**：
- 🔴 P0：`toolDeploy=dedicated`（专用服务器部署）时引擎仍连接目标服务器安装工具，toolServer 配置从未被使用 → 已修复为连接 toolServer 安装
- 🟡 P1：专用服务器地址、堡垒机地址未做必填校验 → 已补全

### 验证项 3：跨云服务商搭建 ✅ 通过

`ssh_ops.py` 的 `_connect_chain` 实现了真实的多跳隧道链路：
- 支持任意 N 跳串联：中继服务器 → 堡垒机 → 零信任网关 → 目标
- 中继/零信任模式：通过 `direct-tcpip` 通道转发（Paramiko 原生隧道）
- 堡垒机模式：支持在已有连接上执行自定义跳转命令（`jumpCommand`）建立新连接
- 跨云场景（如平台在阿里云、Jenkins 在华为云、目标在腾讯云内网）通过中继服务器打通

### 验证项 4：零信任/VPN + 堡垒机访问能力 ✅ 通过（部分平台有局限）

| 能力 | 实现方式 | 状态 |
|------|----------|------|
| 平台自身访问目标服务器 | networkAccess 多跳链路（bastion/zero_trust/relay） | ✅ 全部工具 |
| 生成的 Pipeline 穿透堡垒机 | SSH_OPTIONS 注入 `-J user@bastion:port`（ProxyJump） | ✅ Jenkins/GitLab/GitHub |
| 云厂商托管流水线穿透堡垒机 | — | ⚠️ 阿里云效/华为云/腾讯云生成器未注入 SSH_OPTIONS |

### 验证项 5：参数与配置信息完整性清单 ✅ 齐全

搭建一条完整流水线所需的配置项（前端表单均已覆盖）：

| 类别 | 参数 | 必填 |
|------|------|------|
| 项目 | 仓库 URL、项目名称、默认分支、端口、JDK/Node 版本 | ✅ |
| Git 认证 | 认证方式（密码/SSH 密钥）、用户名、密码/私钥 | 缺失时执行时弹窗 |
| 目标服务器 | 地址、SSH 端口、用户名、认证方式、密码/私钥、部署路径 | 地址必填，其余有默认值或弹窗 |
| 堡垒机（可选） | 启用开关、地址、端口、用户名、认证方式、密码/私钥 | 启用后地址+用户名必填 |
| 网络访问链路（可选） | 每跳：类型（中继/堡垒机/零信任）、地址、端口、用户名、凭据、跳转命令、目标地址 | 配置后凭据缺失弹窗 |
| 工具部署位置 | 专用服务器（地址/端口/用户名/凭据）/ 目标服务器 / 云托管 | dedicated 时服务器地址必填 |
| 发布策略（多分支时） | 发布策略（自动合并/手动审批/双环境并行）、回滚开关、主分支 | 有默认值 |
| 环境选项 | 国内镜像加速（国产 OS 自动启用）、应用服务器类型（Tomcat/TongWeb） | 可选 |

### 验证结论

✅ **核心结论**：项目已具备真实的自动搭建流水线能力——从配置输入到工具安装、流水线配置的全链路均由真实代码执行（非模拟）；配置信息提示齐全，必填项有提交拦截，凭据缺失有运行时弹窗兜底；跨云与零信任/VPN+堡垒机场景通过多跳隧道与 SSH ProxyJump 实现。

⚠️ **已知局限**：
1. 阿里云效/华为云/腾讯云生成器输出简化 YAML 且未注入堡垒机跳转参数
2. 目标服务器仅支持 Linux（apt/yum 系），不支持 Windows 目标
3. 平台自身运行依赖本机 git CLI



